import DTO from "./DTO";
import UserInfoDTO from "./UserInfoDTO";

export default class DTOUtils {
  // static JSONtoDTO(json: JSON): DTO {
  //   if (json.hasOwnProperty("email")) {
  //     return this.JSONtoUserInfoDTO(json);
  //   }
  // }
  //
  // private static JSONtoUserInfoDTO(json: Object): UserInfoDTO {
  //   let email: string;
  //   let name: string;
  //
  //   if (json.hasOwnProperty("email") && json.hasOwnProperty("name")){
  //       return new UserInfoDTO(json.email, json.name);
  //   }
  // }
}
